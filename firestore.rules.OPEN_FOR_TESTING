rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // COMPLETELY OPEN RULES - FOR TESTING ONLY
    // This allows anyone to read/write everything
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
